import fs from 'node:fs';import crypto from 'node:crypto';const m=JSON.parse(fs.readFileSync('checkpoint.json'));const data=Buffer.concat(m.parts.map(p=>fs.readFileSync(p)));if(crypto.createHash('sha256').update(data).digest('hex')!==m.sha256)throw Error('Checksum mismatch');fs.writeFileSync(m.archive,data);console.log('Archive restored. Extract '+m.archive+' then read studio-amir-site/HANDOFF.md');
